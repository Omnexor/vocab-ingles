node_modules/
.vercel/
.env
.env.local
*.log

# El banco de vocabulario SÍ se versiona: es el contenido de la app, no un
# archivo generado que sobre. `npm run vocabulario` lo amplía con la API.

.vercel
.env*
